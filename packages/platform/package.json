{
    "name": "@ccpc/platform",
    "version": "0.1.8",
    "private": false,
    "description": "Platform command interaction layer for editor",
    "license": "MIT",
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "peerDependencies": {
        "@ccpc/canvas": "workspace:*",
        "@ccpc/core": "workspace:*",
        "@ccpc/math": "workspace:*",
        "@ccpc/snap": "workspace:*"
    },
    "scripts": {
        "build": "rimraf dist && pnpm exec vite build && node scripts/patch-dist-package.mjs && node -e \"const { spawnSync } = require('node:child_process'); const r = spawnSync('pnpm', ['exec', 'tsc', '-p', 'tsconfig.build.json', '--emitDeclarationOnly', '--outDir', 'dist/types'], { stdio: 'inherit', shell: true }); if (r.status) console.warn('tsc finished with errors, continue...');\"",
        "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
        "lint": "eslint . --ext .ts",
        "lint:fix": "eslint . --ext .ts --fix",
        "publish": "cd dist && pnpm publish --access public --git-checks=false"
    }
}

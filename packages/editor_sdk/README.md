# @ccpc/editor_sdk

Data layer SDK for the 2D editor.

This package currently provides a minimal state container scaffold so the
editor-specific model can be built incrementally without coupling it to the
rendering or interaction layers.

{
    "name": "@ccpc/core",
    "version": "0.1.3",
    "private": false,
    "description": "Core data model layer for editor",
    "license": "MIT",
    "type": "module",
    "main": "./src/index.ts",
    "types": "./src/index.ts",
    "peerDependencies": {
        "@ccpc/math": "workspace:*"
    },
    "scripts": {
        "build": "rimraf dist && vite build && node scripts/patch-dist-package.mjs && tsc -p tsconfig.json --emitDeclarationOnly --outDir dist/types",
        "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
        "lint": "eslint . --ext .ts",
        "lint:fix": "eslint . --ext .ts --fix",
        "publish": "npm version patch --no-git-tag-version && cd dist && pnpm publish --access public --git-checks=false"
    }
}
